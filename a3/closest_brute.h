#ifndef CLOSEST_BRUTE_H
#define CLOSEST_BRUTE_H

#include <stddef.h>
#include "closest_helpers.h"

/*
 * Brute-force (naive) method to find the distance between the closest
 * pair of poinst among an array of n Point structs. You will use this
 * function 
 */
double brute_force(struct Point P[], size_t n);

#endif /* CLOSEST_BRUTE_H */
